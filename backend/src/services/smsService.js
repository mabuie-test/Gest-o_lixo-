// placeholder: integrate Twilio / or control gateway that triggers SMS via device modem
module.exports = {
  async sendSms(to, message){
    console.log('SMS ->', to, message);
    // implement provider here
  }
};
